{
  "name": "rollenspiel-bewertungen-download",
  "main": "index.js",
  "version": "0.0.2",
  "scripts": {
    "start": "node .",
    "build": "rm -rf build; yarn build:macOS -o build/macOS; yarn build:linux -o build/linux ; yarn build:windows -o build/windows",
    "build:macOS": "pkg -t node14-macos-x64 index.js",
    "build:linux": "pkg -t node14-linux-x64 index.js",
    "build:windows": "pkg -t node14-win-x64 index.js"
  },
  "license": "MIT",
  "private": "false",
  "dependencies": {
    "rss-parser": "^3.12.0"
  },
  "devDependencies": {
    "pkg": "^4.5.1"
  }
}
